this project requires node8 to run to the latest so do this , there should be nvm (node version manager installed with alias for node8) 

	1. nvm use node8
	2. npm i 
	
	this node8 version should only persist for current sesion U should see node 12.14.1 (as latest insatlled) ones this window is closed and new session starts.
